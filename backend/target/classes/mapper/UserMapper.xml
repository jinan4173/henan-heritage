<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.henan.heritage.mapper.UserMapper">

    <resultMap id="userResultMap" type="com.henan.heritage.entity.User">
        <id property="id" column="id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="email" column="email"/>
        <result property="isAdmin" column="is_admin"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="create_time"/>
        <result property="updatedAt" column="update_time"/>
    </resultMap>

    <select id="selectList" resultMap="userResultMap">
        SELECT * FROM user
    </select>

    <select id="selectById" parameterType="java.lang.Long" resultMap="userResultMap">
        SELECT * FROM user WHERE id = #{id}
    </select>

    <select id="selectByUsername" parameterType="java.lang.String" resultMap="userResultMap">
        SELECT * FROM user WHERE username = #{username}
    </select>

    <insert id="insert" parameterType="com.henan.heritage.entity.User">
        INSERT INTO user (username, password, email, is_admin, status, create_time, update_time)
        VALUES (#{username}, #{password}, #{email}, #{isAdmin}, #{status}, NOW(), NOW())
    </insert>

    <update id="update" parameterType="com.henan.heritage.entity.User">
        UPDATE user
        SET username = #{username},
            email = #{email},
            is_admin = #{isAdmin},
            status = #{status},
            update_time = NOW()
        <if test="password != null">
            , password = #{password}
        </if>
        WHERE id = #{id}
    </update>

    <delete id="delete" parameterType="java.lang.Long">
        DELETE FROM user WHERE id = #{id}
    </delete>

    <select id="count" resultType="java.lang.Long">
        SELECT COUNT(*) FROM user
    </select>

</mapper>