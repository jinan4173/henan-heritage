<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.henan.heritage.mapper.AdminUserMapper">
    <select id="findByUsername" resultType="com.henan.heritage.entity.AdminUser">
        SELECT * FROM admin_user WHERE username = #{username}
    </select>
    <select id="selectById" resultType="com.henan.heritage.entity.AdminUser">
        SELECT * FROM admin_user WHERE id = #{id}
    </select>
    <insert id="insert" parameterType="com.henan.heritage.entity.AdminUser">
        INSERT INTO admin_user (username, password, create_time, update_time) VALUES (#{username}, #{password}, NOW(), NOW())
    </insert>
    <update id="update" parameterType="com.henan.heritage.entity.AdminUser">
        UPDATE admin_user SET username = #{username}, password = #{password}, update_time = NOW() WHERE id = #{id}
    </update>
    <delete id="delete" parameterType="java.lang.Long">
        DELETE FROM admin_user WHERE id = #{id}
    </delete>
</mapper>